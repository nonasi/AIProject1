Stumped on the CornersProblem:

-Where does the "state" get defined?
    Should it be its own class?

-What should the Hash function look like?

-What should the state look like: maybe ( (x,y), [goal1, goal3], g(n) ) ?

-
